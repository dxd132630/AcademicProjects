public class nonRepeatedCharacterInArray{
	public static void findNonRepeatedCharacterInArray(String input){
		HashMap<Character, Integer> hashInput = new HashMap<>();
		for(int i=0;i<input.length();i++){
			char c = word.CharAt(i);
			if(hashInput.constainsKey(c))
				hashInput.put(c,hashInput.get(c)+1);
			else
				hashInput.put(c,1);
		}
		for(int i=0;i<hashInput.size;i++){
			
		}
	}
	public static void main(String args[]){
		String input="Hello";
		findNonepeatedCharacterInArray(input);
	}	
}
